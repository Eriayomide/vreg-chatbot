version: "3.8"

services:
  backend:
    build: ./backend
    container_name: vreg-backend
    ports:
      - "8083:8083"
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: vreg-frontend
    ports:
      - "8003:80"
    restart: unless-stopped
